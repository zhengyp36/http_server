# http_server
simple http server by python

# create http_server for downloading
python -m http.server --directory $dir $port

# create http_server for uploading
python -m http_upload_server.py --directory $dir $port
